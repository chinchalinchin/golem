# Golem Common Configuration
# Defines the "Superset" of abilities for the LNN.

doom_map = map01

# Rewards 
living_reward = -1

# Input Space (The Superset)
# The LNN will always output a vector of size 8.
available_buttons = 
    { 
        MOVE_FORWARD
        MOVE_BACKWARD
        MOVE_LEFT 
        MOVE_RIGHT
        TURN_LEFT
        TURN_RIGHT
        ATTACK
        USE
    }

# State Space
available_game_variables = { AMMO2 HEALTH ARMOR }

# Game Settings
mode = SPECTATOR
doom_skill = 5